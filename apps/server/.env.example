# Server port
PORT=3000

# Embedding server configuration
# Set to "true" to use Modal.com cloud embeddings (skips local Python server)
USE_MODAL_EMBEDDINGS=true

# URL of the embedding server (Modal or local)
# For Modal: https://your-username--jina-clip-v2-fastapi-app.modal.run
# For local: http://localhost:8001
PYTHON_EMBED_URL=https://isblu--jina-clip-v2-fastapi-app.modal.run
